apiVersion: v1
kind: Service
metadata:
  name: {{ include "hike-blog.fullname" . }}
  labels:
    {{- include "hike-blog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hike-blog.selectorLabels" . | nindent 4 }}

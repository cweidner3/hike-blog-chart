apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hike-blog.fullname" . }}-test-connection"
  labels:
    {{- include "hike-blog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hike-blog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
